// if else are only for two conditions... if there are more options thrn we have to nest it so in that case we use switch cases... in each case we have to use break;
// Example:
let a=25;
let b=5;
let operator = "+";
switch(operator){
case "+":
console.log(a+b);
break;
case "-":
console.log(a-b);
break;
case "*":
console.log(a*b);
break;
case "/":
console.log(a/b);
break;
}
 